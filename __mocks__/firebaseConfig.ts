export const auth = {
  currentUser: null,
};

export const db = {};

export const app = {};

export const registerWithEmailAndPassword = jest.fn(async () => {
  // simulate a successful register
});

export const logout = jest.fn(() => {});
